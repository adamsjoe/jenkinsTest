pipeline {

    agent any
    stages {
        stage('build') {
            steps {
                git branch: 'main',
                url: 'https://github.com/adamsjoe/jenkinsTest.git'
            }

            things = load( "${env.WORKSPACE}/otherStuff.groovy" )            
        }

        things.build()

        stage('la de dah') {
            steps {
                echo 'bleugh'
            }
        }

        this.result()
    }
}
